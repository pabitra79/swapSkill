<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <style>
      :root {
        --admin-primary: #2c3e50;
        --admin-secondary: #34495e;
        --admin-accent: #3498db;
        --admin-success: #27ae60;
        --admin-warning: #f39c12;
        --admin-danger: #e74c3c;
        --admin-info: #1abc9c;
        --admin-light: #ecf0f1;
        --admin-dark: #2c3e50;
      }

      body {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }

      /* Admin Layout */
      .admin-container {
        background: rgba(255, 255, 255, 0.95);
        border-radius: 20px;
        margin: 20px;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
        backdrop-filter: blur(10px);
        min-height: calc(100vh - 40px);
        overflow: hidden;
      }

      /* Sidebar */
      .admin-sidebar {
        background: linear-gradient(
          180deg,
          var(--admin-primary) 0%,
          var(--admin-secondary) 100%
        );
        color: white;
        padding: 0;
        min-height: 100vh;
        box-shadow: 4px 0 20px rgba(0, 0, 0, 0.1);
      }

      .admin-brand {
        padding: 2rem 1.5rem;
        background: rgba(0, 0, 0, 0.2);
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      }

      .admin-brand h3 {
        margin: 0;
        font-weight: 700;
        background: linear-gradient(45deg, #3498db, #1abc9c);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .admin-nav .nav-link {
        color: rgba(255, 255, 255, 0.8);
        padding: 1rem 1.5rem;
        border-left: 4px solid transparent;
        transition: all 0.3s ease;
        font-weight: 500;
        margin: 5px 0;
      }

      .admin-nav .nav-link:hover {
        color: white;
        background: rgba(255, 255, 255, 0.1);
        border-left-color: var(--admin-accent);
        transform: translateX(5px);
      }

      .admin-nav .nav-link.active {
        color: white;
        background: rgba(255, 255, 255, 0.15);
        border-left-color: var(--admin-success);
        box-shadow: inset 0 0 20px rgba(255, 255, 255, 0.1);
      }

      .admin-nav .nav-link i {
        width: 25px;
        text-align: center;
        margin-right: 10px;
        font-size: 1.1em;
      }

      /* Main Content */
      .admin-main {
        padding: 2rem;
        background: var(--admin-light);
        min-height: 100vh;
      }

      /* Header */
      .admin-header {
        background: white;
        padding: 1.5rem 2rem;
        border-radius: 15px;
        box-shadow: 0 5px 20px rgba(0, 0, 0, 0.08);
        margin-bottom: 2rem;
        border-left: 5px solid var(--admin-info);
      }

      .admin-header h1 {
        color: var(--admin-primary);
        font-weight: 800;
        margin: 0;
        font-size: 2.2rem;
      }

      .admin-header p {
        color: #7f8c8d;
        margin: 0.5rem 0 0 0;
        font-size: 1.1rem;
      }

      /* Search Box */
      .search-box {
        position: relative;
        max-width: 400px;
      }

      .search-box .form-control {
        border-radius: 25px;
        padding: 0.75rem 1.5rem;
        border: 2px solid #e9ecef;
        transition: all 0.3s ease;
      }

      .search-box .form-control:focus {
        border-color: var(--admin-accent);
        box-shadow: 0 0 0 0.2rem rgba(52, 152, 219, 0.25);
      }

      .search-box .search-icon {
        position: absolute;
        right: 15px;
        top: 50%;
        transform: translateY(-50%);
        color: #6c757d;
      }

      /* Users Table Card */
      .users-card {
        background: white;
        border: none;
        border-radius: 15px;
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
        overflow: hidden;
      }

      .users-card-header {
        background: linear-gradient(
          135deg,
          var(--admin-primary),
          var(--admin-secondary)
        );
        color: white;
        padding: 1.5rem;
        border-bottom: none;
      }

      .users-card-header h5 {
        margin: 0;
        font-weight: 700;
        font-size: 1.2rem;
      }

      .users-count {
        background: rgba(255, 255, 255, 0.2);
        padding: 0.25rem 0.75rem;
        border-radius: 15px;
        font-size: 0.9rem;
      }

      /* Table Styling */
      .table-users {
        margin: 0;
      }

      .table-users thead th {
        background: #f8f9fa;
        border-bottom: 2px solid #dee2e6;
        font-weight: 700;
        color: var(--admin-primary);
        padding: 1rem;
        text-transform: uppercase;
        font-size: 0.85rem;
        letter-spacing: 0.5px;
      }

      .table-users tbody tr {
        transition: all 0.3s ease;
        border-left: 3px solid transparent;
      }

      .table-users tbody tr:hover {
        background: #f8f9fa;
        border-left-color: var(--admin-accent);
        transform: translateX(5px);
      }

      .table-users tbody td {
        padding: 1rem;
        vertical-align: middle;
        border-color: #f1f3f4;
      }

      /* Badge Styles */
      .badge-admin {
        background: linear-gradient(45deg, var(--admin-danger), #c0392b);
        color: white;
        padding: 0.5rem 0.75rem;
        border-radius: 20px;
        font-size: 0.75rem;
        font-weight: 600;
      }

      .badge-user {
        background: linear-gradient(
          45deg,
          var(--admin-primary),
          var(--admin-secondary)
        );
        color: white;
        padding: 0.5rem 0.75rem;
        border-radius: 20px;
        font-size: 0.75rem;
        font-weight: 600;
      }

      .badge-verified {
        background: linear-gradient(45deg, var(--admin-success), #229954);
        color: white;
        padding: 0.5rem 0.75rem;
        border-radius: 20px;
        font-size: 0.75rem;
        font-weight: 600;
      }

      .badge-unverified {
        background: linear-gradient(45deg, var(--admin-warning), #e67e22);
        color: white;
        padding: 0.5rem 0.75rem;
        border-radius: 20px;
        font-size: 0.75rem;
        font-weight: 600;
      }

      /* Action Buttons */
      .btn-action {
        width: 35px;
        height: 35px;
        border-radius: 50%;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        margin: 0 2px;
        transition: all 0.3s ease;
        border: none;
      }

      .btn-view {
        background: var(--admin-info);
        color: white;
      }

      .btn-delete {
        background: var(--admin-danger);
        color: white;
      }

      .btn-action:hover {
        transform: scale(1.1);
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
      }

      /* Stats Cards */
      .stat-card-mini {
        background: white;
        border-radius: 10px;
        padding: 1rem;
        text-align: center;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        border-left: 4px solid var(--admin-info);
      }

      .stat-number-mini {
        font-size: 1.5rem;
        font-weight: 800;
        color: var(--admin-primary);
        margin-bottom: 0.25rem;
      }

      .stat-label-mini {
        color: #7f8c8d;
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 0.5px;
      }

      /* Modal Styling */
      .modal-user {
        border-radius: 15px;
        border: none;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      }

      .modal-user .modal-header {
        background: linear-gradient(
          135deg,
          var(--admin-primary),
          var(--admin-secondary)
        );
        color: white;
        border-radius: 15px 15px 0 0;
        border-bottom: none;
        padding: 1.5rem;
      }

      .modal-user .modal-body {
        padding: 2rem;
      }

      .user-avatar {
        width: 80px;
        height: 80px;
        border-radius: 50%;
        object-fit: cover;
        border: 4px solid #f8f9fa;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      }

      .skill-tag {
        background: #e9ecef;
        color: #495057;
        padding: 0.5rem 1rem;
        border-radius: 20px;
        margin: 0.25rem;
        display: inline-block;
        font-size: 0.85rem;
      }

      /* Responsive */
      @media (max-width: 768px) {
        .admin-container {
          margin: 10px;
          border-radius: 15px;
        }

        .admin-main {
          padding: 1rem;
        }

        .table-responsive {
          font-size: 0.9rem;
        }

        .search-box {
          max-width: 100%;
          margin-top: 1rem;
        }
      }
    </style>
  </head>
  <body>
    <div class="admin-container">
      <div class="row g-0">
        <!-- Sidebar -->
        <div class="col-md-2 admin-sidebar">
          <div class="admin-brand">
            <h3><i class="fas fa-shield-alt me-2"></i>Admin Panel</h3>
          </div>
          <nav class="admin-nav mt-4">
            <a href="/admin/dashboard" class="nav-link">
              <i class="fas fa-tachometer-alt"></i> Dashboard
            </a>
            <a href="/admin/users" class="nav-link active">
              <i class="fas fa-users"></i> Users
            </a>

            <div class="mt-5 pt-4 border-top border-secondary">
              <a href="/dashboard" class="nav-link text-warning">
                <i class="fas fa-arrow-left"></i> Back to User View
              </a>
            </div>
          </nav>
        </div>

        <!-- Main Content -->
        <div class="col-md-10 admin-main">
          <!-- Header -->
          <div class="admin-header">
            <div class="row align-items-center">
              <div class="col-md-6">
                <h1><i class="fas fa-users me-3"></i>Manage Users</h1>
                <p>Monitor and manage all SkillSwap users</p>
              </div>
              <div class="col-md-6">
                <div class="search-box">
                  <input
                    type="text"
                    id="searchInput"
                    class="form-control"
                    placeholder="Search users by name or email..."
                  />
                  <i class="fas fa-search search-icon"></i>
                </div>
              </div>
            </div>
          </div>

          <!-- Quick Stats -->
          <div class="row mb-4">
            <div class="col-md-3">
              <div class="stat-card-mini">
                <div class="stat-number-mini"><%= users.length %></div>
                <div class="stat-label-mini">Total Users</div>
              </div>
            </div>
            <div class="col-md-3">
              <div class="stat-card-mini">
                <div class="stat-number-mini">
                  <%= users.filter(u => u.role === 'admin').length %>
                </div>
                <div class="stat-label-mini">Admins</div>
              </div>
            </div>
            <div class="col-md-3">
              <div class="stat-card-mini">
                <div class="stat-number-mini">
                  <%= users.filter(u => u.isVerified).length %>
                </div>
                <div class="stat-label-mini">Verified</div>
              </div>
            </div>
            <div class="col-md-3">
              <div class="stat-card-mini">
                <div class="stat-number-mini">
                  <%= users.reduce((sum, user) => sum +
                  (user.stats?.completedSessions || 0), 0) %>
                </div>
                <div class="stat-label-mini">Total Sessions</div>
              </div>
            </div>
          </div>

          <!-- Users Table -->
          <div class="users-card">
            <div class="users-card-header">
              <div class="d-flex justify-content-between align-items-center">
                <h5><i class="fas fa-list me-2"></i>Users List</h5>
                <span class="users-count"><%= users.length %> users</span>
              </div>
            </div>
            <div class="card-body p-0">
              <div class="table-responsive">
                <table class="table table-users">
                  <thead>
                    <tr>
                      <th>User</th>
                      <th>Contact</th>
                      <th>Role</th>
                      <th>Status</th>
                      <th>Joined</th>
                      <th>Activity</th>
                      <th>Actions</th>
                    </tr>
                  </thead>
                  <tbody id="usersTable">
                    <% users.forEach(user => { %>
                    <tr data-user-id="<%= user._id %>">
                      <td>
                        <div class="d-flex align-items-center">
                          <% if (user.profile?.avatar) { %>
                          <img
                            src="<%= user.profile.avatar %>"
                            class="user-avatar me-3"
                            alt="<%= user.name %>"
                          />
                          <% } else { %>
                          <div
                            class="user-avatar bg-primary text-white d-flex align-items-center justify-content-center me-3"
                          >
                            <%= user.name.charAt(0).toUpperCase() %>
                          </div>
                          <% } %>
                          <div>
                            <strong class="d-block"><%= user.name %></strong>
                            <small class="text-muted">
                              <% if (user.profile?.location) { %>
                              <i class="fas fa-map-marker-alt me-1"></i><%=
                              user.profile.location %> <% } else { %> Location
                              not set <% } %>
                            </small>
                          </div>
                        </div>
                      </td>
                      <td>
                        <div class="text-muted"><%= user.email %></div>
                        <% if (user.profile?.website) { %>
                        <small
                          ><i class="fas fa-globe me-1"></i><%=
                          user.profile.website %></small
                        >
                        <% } %>
                      </td>
                      <td>
                        <% if (user.role === 'admin') { %>
                        <span class="badge-admin">Admin</span>
                        <% } else { %>
                        <span class="badge-user">User</span>
                        <% } %>
                      </td>
                      <td>
                        <% if (user.isVerified) { %>
                        <span class="badge-verified">Verified</span>
                        <% } else { %>
                        <span class="badge-unverified">Unverified</span>
                        <% } %>
                      </td>
                      <td>
                        <small class="text-muted">
                          <%= new Date(user.createdAt).toLocaleDateString() %>
                        </small>
                      </td>
                      <td>
                        <div class="small">
                          <div>
                            <i class="fas fa-handshake me-1"></i> <%=
                            user.stats?.completedSessions || 0 %> sessions
                          </div>
                          <div>
                            <i class="fas fa-clock me-1"></i> <%=
                            user.stats?.hoursTaught || 0 %>h taught
                          </div>
                        </div>
                      </td>
                      <td>
                        <button
                          class="btn-action btn-view"
                          onclick="viewUser('<%= user._id %>')"
                          title="View Details"
                        >
                          <i class="fas fa-eye"></i>
                        </button>
                        <% if (user.role !== 'admin') { %>
                        <button
                          class="btn-action btn-delete"
                          onclick="deleteUser('<%= user._id %>', '<%= user.name %>')"
                          title="Delete User"
                        >
                          <i class="fas fa-trash"></i>
                        </button>
                        <% } %>
                      </td>
                    </tr>
                    <% }); %>
                  </tbody>
                </table>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- User Details Modal -->
    <div class="modal fade" id="userModal" tabindex="-1">
      <div class="modal-dialog modal-lg modal-user">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title">
              <i class="fas fa-user me-2"></i>User Details
            </h5>
            <button
              type="button"
              class="btn-close btn-close-white"
              data-bs-dismiss="modal"
            ></button>
          </div>
          <div class="modal-body" id="userDetails">
            <!-- User details will be loaded here -->
          </div>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
      // View user details
      async function viewUser(userId) {
        try {
          const response = await fetch(`/admin/users/${userId}`);
          const user = await response.json();

          document.getElementById("userDetails").innerHTML = `
            <div class="row">
              <div class="col-md-4 text-center mb-4">
                ${
                  user.profile?.avatar
                    ? `<img src="${user.profile.avatar}" class="user-avatar" alt="${user.name}">`
                    : `<div class="user-avatar bg-primary text-white d-flex align-items-center justify-content-center mx-auto">
                    ${user.name.charAt(0).toUpperCase()}
                  </div>`
                }
                <h4 class="mt-3">${user.name}</h4>
                <p class="text-muted">${user.email}</p>
                <div class="mb-2">
                  ${
                    user.role === "admin"
                      ? '<span class="badge-admin">Administrator</span>'
                      : '<span class="badge-user">Regular User</span>'
                  }
                  ${
                    user.isVerified
                      ? '<span class="badge-verified ms-2">Verified</span>'
                      : '<span class="badge-unverified ms-2">Unverified</span>'
                  }
                </div>
              </div>
              <div class="col-md-8">
                <div class="row">
                  <div class="col-md-6">
                    <h6><i class="fas fa-chart-bar me-2"></i>Statistics</h6>
                    <div class="mb-3">
                      <strong>Sessions Completed:</strong> ${
                        user.stats?.completedSessions || 0
                      }<br>
                      <strong>Hours Taught:</strong> ${
                        user.stats?.hoursTaught || 0
                      }<br>
                      <strong>Hours Learned:</strong> ${
                        user.stats?.hoursLearned || 0
                      }<br>
                      <strong>Response Rate:</strong> ${
                        user.stats?.responseRate || 0
                      }%
                    </div>
                  </div>
                  <div class="col-md-6">
                    <h6><i class="fas fa-info-circle me-2"></i>Profile</h6>
                    <div class="mb-3">
                      <strong>Location:</strong> ${
                        user.profile?.location || "Not set"
                      }<br>
                      <strong>Joined:</strong> ${new Date(
                        user.createdAt
                      ).toLocaleDateString()}<br>
                      <strong>Timezone:</strong> ${
                        user.profile?.timezone || "Not set"
                      }<br>
                      <strong>Language:</strong> ${
                        user.profile?.language || "Not set"
                      }
                    </div>
                  </div>
                </div>
                
                <div class="row mt-3">
                  <div class="col-md-6">
                    <h6><i class="fas fa-chalkboard-teacher me-2"></i>Teaching Skills</h6>
                    <div>
                      ${
                        user.profile?.teachSkills &&
                        user.profile.teachSkills.length > 0
                          ? user.profile.teachSkills
                              .map(
                                (skill) =>
                                  `<span class="skill-tag">${skill}</span>`
                              )
                              .join("")
                          : '<span class="text-muted">No teaching skills added</span>'
                      }
                    </div>
                  </div>
                  <div class="col-md-6">
                    <h6><i class="fas fa-graduation-cap me-2"></i>Learning Skills</h6>
                    <div>
                      ${
                        user.profile?.learnSkills &&
                        user.profile.learnSkills.length > 0
                          ? user.profile.learnSkills
                              .map(
                                (skill) =>
                                  `<span class="skill-tag">${skill}</span>`
                              )
                              .join("")
                          : '<span class="text-muted">No learning goals added</span>'
                      }
                    </div>
                  </div>
                </div>

                ${
                  user.profile?.bio
                    ? `
                <div class="row mt-3">
                  <div class="col-12">
                    <h6><i class="fas fa-user me-2"></i>Bio</h6>
                    <p class="text-muted">${user.profile.bio}</p>
                  </div>
                </div>
                `
                    : ""
                }
              </div>
            </div>
          `;

          new bootstrap.Modal(document.getElementById("userModal")).show();
        } catch (error) {
          alert("Error loading user details");
        }
      }

      // Delete user
      async function deleteUser(userId, userName) {
        if (
          !confirm(
            `Are you sure you want to delete ${userName}? This action cannot be undone and will delete all their sessions and swap requests.`
          )
        ) {
          return;
        }

        try {
          const response = await fetch(`/admin/users/${userId}`, {
            method: "DELETE",
          });

          const data = await response.json();

          if (response.ok) {
            alert(` ${data.message}`);
            document.querySelector(`tr[data-user-id="${userId}"]`).remove();
          } else {
            alert(` ${data.error || "Error deleting user"}`);
          }
        } catch (error) {
          alert(" Error deleting user");
        }
      }

      // Search functionality
      document
        .getElementById("searchInput")
        .addEventListener("input", async (e) => {
          const query = e.target.value.trim();

          if (query.length < 2) {
            // Reset to original data if search is cleared
            if (query.length === 0) {
              window.location.reload();
            }
            return;
          }

          try {
            const response = await fetch(
              `/admin/users/search?q=${encodeURIComponent(query)}`
            );
            const users = await response.json();

            const tbody = document.getElementById("usersTable");
            tbody.innerHTML = users
              .map(
                (user) => `
            <tr data-user-id="${user._id}">
              <td>
                <div class="d-flex align-items-center">
                  ${
                    user.profile?.avatar
                      ? `<img src="${user.profile.avatar}" class="user-avatar me-3" alt="${user.name}">`
                      : `<div class="user-avatar bg-primary text-white d-flex align-items-center justify-content-center me-3">
                      ${user.name.charAt(0).toUpperCase()}
                    </div>`
                  }
                  <div>
                    <strong class="d-block">${user.name}</strong>
                    <small class="text-muted">
                      ${
                        user.profile?.location
                          ? `<i class="fas fa-map-marker-alt me-1"></i>${user.profile.location}`
                          : "Location not set"
                      }
                    </small>
                  </div>
                </div>
              </td>
              <td>
                <div class="text-muted">${user.email}</div>
                ${
                  user.profile?.website
                    ? `<small><i class="fas fa-globe me-1"></i>${user.profile.website}</small>`
                    : ""
                }
              </td>
              <td>
                ${
                  user.role === "admin"
                    ? '<span class="badge-admin">Admin</span>'
                    : '<span class="badge-user">User</span>'
                }
              </td>
              <td>
                ${
                  user.isVerified
                    ? '<span class="badge-verified">Verified</span>'
                    : '<span class="badge-unverified">Unverified</span>'
                }
              </td>
              <td>
                <small class="text-muted">${new Date(
                  user.createdAt
                ).toLocaleDateString()}</small>
              </td>
              <td>
                <div class="small">
                  <div><i class="fas fa-handshake me-1"></i> ${
                    user.stats?.completedSessions || 0
                  } sessions</div>
                  <div><i class="fas fa-clock me-1"></i> ${
                    user.stats?.hoursTaught || 0
                  }h taught</div>
                </div>
              </td>
              <td>
                <button class="btn-action btn-view" onclick="viewUser('${
                  user._id
                }')" title="View Details">
                  <i class="fas fa-eye"></i>
                </button>
                ${
                  user.role !== "admin"
                    ? `<button class="btn-action btn-delete" onclick="deleteUser('${user._id}', '${user.name}')" title="Delete User">
                    <i class="fas fa-trash"></i>
                  </button>`
                    : ""
                }
              </td>
            </tr>
          `
              )
              .join("");
          } catch (error) {
            console.error("Search error:", error);
          }
        });
    </script>
  </body>
</html>
