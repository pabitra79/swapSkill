<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <style>
      :root {
        --admin-primary: #2c3e50;
        --admin-secondary: #34495e;
        --admin-accent: #3498db;
        --admin-success: #27ae60;
        --admin-warning: #f39c12;
        --admin-danger: #e74c3c;
        --admin-info: #1abc9c;
        --admin-light: #ecf0f1;
        --admin-dark: #2c3e50;
      }

      body {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }

      /* Admin Layout */
      .admin-container {
        background: rgba(255, 255, 255, 0.95);
        border-radius: 20px;
        margin: 20px;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
        backdrop-filter: blur(10px);
        min-height: calc(100vh - 40px);
        overflow: hidden;
      }

      /* Sidebar */
      .admin-sidebar {
        background: linear-gradient(
          180deg,
          var(--admin-primary) 0%,
          var(--admin-secondary) 100%
        );
        color: white;
        padding: 0;
        min-height: 100vh;
        box-shadow: 4px 0 20px rgba(0, 0, 0, 0.1);
      }

      .admin-brand {
        padding: 2rem 1.5rem;
        background: rgba(0, 0, 0, 0.2);
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      }

      .admin-brand h3 {
        margin: 0;
        font-weight: 700;
        background: linear-gradient(45deg, #3498db, #1abc9c);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .admin-nav .nav-link {
        color: rgba(255, 255, 255, 0.8);
        padding: 1rem 1.5rem;
        border-left: 4px solid transparent;
        transition: all 0.3s ease;
        font-weight: 500;
        margin: 5px 0;
      }

      .admin-nav .nav-link:hover {
        color: white;
        background: rgba(255, 255, 255, 0.1);
        border-left-color: var(--admin-accent);
        transform: translateX(5px);
      }

      .admin-nav .nav-link.active {
        color: white;
        background: rgba(255, 255, 255, 0.15);
        border-left-color: var(--admin-success);
        box-shadow: inset 0 0 20px rgba(255, 255, 255, 0.1);
      }

      .admin-nav .nav-link i {
        width: 25px;
        text-align: center;
        margin-right: 10px;
        font-size: 1.1em;
      }

      /* Main Content */
      .admin-main {
        padding: 2rem;
        background: var(--admin-light);
        min-height: 100vh;
      }

      /* Header */
      .admin-header {
        background: white;
        padding: 1.5rem 2rem;
        border-radius: 15px;
        box-shadow: 0 5px 20px rgba(0, 0, 0, 0.08);
        margin-bottom: 2rem;
        border-left: 5px solid var(--admin-accent);
      }

      .admin-header h1 {
        color: var(--admin-primary);
        font-weight: 800;
        margin: 0;
        font-size: 2.2rem;
      }

      .admin-header p {
        color: #7f8c8d;
        margin: 0.5rem 0 0 0;
        font-size: 1.1rem;
      }

      /* Stats Cards */
      .stat-card {
        background: white;
        border: none;
        border-radius: 15px;
        padding: 1.5rem;
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
        border-left: 4px solid;
        height: 100%;
        position: relative;
        overflow: hidden;
      }

      .stat-card::before {
        content: "";
        position: absolute;
        top: 0;
        right: 0;
        width: 80px;
        height: 80px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 50%;
        transform: translate(30px, -30px);
      }

      .stat-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 35px rgba(0, 0, 0, 0.15);
      }

      .stat-card.primary {
        border-left-color: var(--admin-primary);
      }
      .stat-card.success {
        border-left-color: var(--admin-success);
      }
      .stat-card.info {
        border-left-color: var(--admin-info);
      }
      .stat-card.warning {
        border-left-color: var(--admin-warning);
      }

      .stat-icon {
        width: 60px;
        height: 60px;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        margin-bottom: 1rem;
        color: white;
      }

      .stat-icon.primary {
        background: var(--admin-primary);
      }
      .stat-icon.success {
        background: var(--admin-success);
      }
      .stat-icon.info {
        background: var(--admin-info);
      }
      .stat-icon.warning {
        background: var(--admin-warning);
      }

      .stat-number {
        font-size: 2.5rem;
        font-weight: 800;
        margin-bottom: 0.5rem;
        color: var(--admin-primary);
      }

      .stat-label {
        color: #7f8c8d;
        font-weight: 600;
        font-size: 0.9rem;
        text-transform: uppercase;
        letter-spacing: 0.5px;
      }

      .stat-subtext {
        color: #95a5a6;
        font-size: 0.85rem;
        margin-top: 0.5rem;
      }

      /* Content Cards */
      .content-card {
        background: white;
        border: none;
        border-radius: 15px;
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
        margin-bottom: 2rem;
        overflow: hidden;
      }

      .content-card-header {
        background: linear-gradient(
          135deg,
          var(--admin-primary),
          var(--admin-secondary)
        );
        color: white;
        padding: 1.5rem;
        border-bottom: none;
      }

      .content-card-header h5 {
        margin: 0;
        font-weight: 700;
        font-size: 1.2rem;
      }

      .content-card-body {
        padding: 1.5rem;
      }

      /* List Items */
      .activity-item {
        padding: 1rem;
        border-bottom: 1px solid #ecf0f1;
        transition: all 0.3s ease;
      }

      .activity-item:hover {
        background: #f8f9fa;
        transform: translateX(5px);
      }

      .activity-item:last-child {
        border-bottom: none;
      }

      .skill-badge {
        background: linear-gradient(
          45deg,
          var(--admin-primary),
          var(--admin-secondary)
        );
        color: white;
        padding: 8px 15px;
        border-radius: 25px;
        font-weight: 600;
        font-size: 0.85rem;
        box-shadow: 0 4px 15px rgba(44, 62, 80, 0.2);
      }

      /* Responsive */
      @media (max-width: 768px) {
        .admin-container {
          margin: 10px;
          border-radius: 15px;
        }

        .admin-main {
          padding: 1rem;
        }

        .stat-number {
          font-size: 2rem;
        }
        /* Admin Topbar */
.admin-topbar {
  background: white;
  border-radius: 15px;
  box-shadow: 0 5px 20px rgba(0, 0, 0, 0.08);
  margin-bottom: 2rem;
}

.admin-avatar {
  width: 40px;
  height: 40px;
}

.admin-topbar .btn {
  border-radius: 20px;
  padding: 0.5rem 1rem;
  font-weight: 500;
  transition: all 0.3s ease;
}

.admin-topbar .btn:hover {
  transform: translateY(-1px);
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
}
      }
    </style>
  </head>
  <body>
    <div class="admin-container">
      <div class="row g-0">
        <div class="col-md-2 admin-sidebar">
          <div class="admin-brand">
            <h3><i class="fas fa-shield-alt me-2"></i>Admin Panel</h3>
          </div>
          <nav class="admin-nav mt-4">
            <a href="/admin/dashboard" class="nav-link active">
              <i class="fas fa-tachometer-alt"></i> Dashboard
            </a>
            <a href="/admin/users" class="nav-link">
              <i class="fas fa-users"></i> Users
            </a>
            <div class="mt-5 pt-4 border-top border-secondary">
              <a href="/dashboard" class="nav-link text-warning">
                <i class="fas fa-arrow-left"></i> Back to User View
              </a>
            </div>
            
          </nav>
        </div>


        <div class="col-md-10 admin-main">
          <!-- Header -->
          <div class="admin-header">
            <h1>🛠️ Admin Dashboard</h1>
            <p>Monitor and manage your SkillSwap platform</p>
          </div>
<div class="col-md-10 admin-main">
  <div class="admin-topbar">
    <div class="d-flex justify-content-between align-items-center p-3 bg-white shadow-sm rounded mb-4">
      <div class="d-flex align-items-center">
        <div class="admin-avatar me-3">
          <% if (user.profile && user.profile.avatar) { %>
            <img src="<%= user.profile.avatar %>" alt="<%= user.name %>" class="rounded-circle" style="width: 40px; height: 40px; object-fit: cover;">
          <% } else { %>
            <div class="bg-primary text-white rounded-circle d-flex align-items-center justify-content-center" style="width: 40px; height: 40px; font-size: 1rem;">
              <%= user.name.charAt(0).toUpperCase() %>
            </div>
          <% } %>
        </div>
        <div>
          <h6 class="mb-0 fw-bold text-dark"><%= user.name %></h6>
          <small class="text-muted">Administrator</small>
        </div>
      </div>
      <div class="d-flex align-items-center gap-3">
        <span class="text-muted">
          <i class="fas fa-shield-alt me-1"></i>Admin Mode
        </span>
        <div class="vr"></div>
        <a href="/dashboard" class="btn btn-outline-primary btn-sm">
          <i class="fas fa-user me-1"></i>User View
        </a>
        <form action="/admin/logout" method="POST" class="d-inline">
          <button type="submit" class="btn btn-outline-danger btn-sm">
            <i class="fas fa-sign-out-alt me-1"></i>Logout
          </button>
        </form>
      </div>
    </div>

   
    <div class="admin-header">
     
    </div>

          <div class="row g-4 mb-5">
            <div class="col-md-3">
              <div class="stat-card primary">
                <div class="stat-icon primary">
                  <i class="fas fa-users"></i>
                </div>
                <div class="stat-number"><%= stats.totalUsers || 0 %></div>
                <div class="stat-label">Total Users</div>
                <div class="stat-subtext">
                  <%= stats.verifiedUsers || 0 %> verified accounts
                </div>
              </div>
            </div>

            <div class="col-md-3">
              <div class="stat-card success">
                <div class="stat-icon success">
                  <i class="fas fa-handshake"></i>
                </div>
                <div class="stat-number"><%= stats.totalSessions || 0 %></div>
                <div class="stat-label">Total Sessions</div>
                <div class="stat-subtext">
                  <%= stats.totalHours || 0 %> hours exchanged
                </div>
              </div>
            </div>

            <div class="col-md-3">
              <div class="stat-card info">
                <div class="stat-icon info">
                  <i class="fas fa-exchange-alt"></i>
                </div>
                <div class="stat-number">
                  <%= stats.totalSwapRequests || 0 %>
                </div>
                <div class="stat-label">Swap Requests</div>
                <div class="stat-subtext">
                  <%= stats.acceptedSwaps || 0 %> accepted
                </div>
              </div>
            </div>

            <div class="col-md-3">
              <div class="stat-card warning">
                <div class="stat-icon warning">
                  <i class="fas fa-chart-line"></i>
                </div>
                <div class="stat-number">
                  <% if (stats.totalSwapRequests > 0) { %> <%=
                  ((stats.acceptedSwaps / stats.totalSwapRequests) *
                  100).toFixed(0) %>% <% } else { %> 0% <% } %>
                </div>
                <div class="stat-label">Acceptance Rate</div>
                <div class="stat-subtext">Successful swaps</div>
              </div>
            </div>
          </div>

          <!-- Recent Activities -->
          <div class="row g-4">
            <div class="col-md-6">
              <div class="content-card">
                <div class="content-card-header">
                  <h5><i class="fas fa-user-plus me-2"></i>Recent Users</h5>
                </div>
                <div class="content-card-body">
                  <% if (activities.recentUsers && activities.recentUsers.length
                  > 0) { %> <% activities.recentUsers.forEach(user => { %>
                  <div class="activity-item">
                    <div
                      class="d-flex justify-content-between align-items-start"
                    >
                      <div>
                        <strong class="text-primary"><%= user.name %></strong>
                        <div class="text-muted small"><%= user.email %></div>
                        <div class="text-muted small mt-1">
                          <i class="fas fa-calendar me-1"></i>
                          Joined <%= new
                          Date(user.createdAt).toLocaleDateString() %>
                        </div>
                      </div>
                      <span class="badge bg-success">Active</span>
                    </div>
                  </div>
                  <% }); %> <% } else { %>
                  <div class="text-center py-4">
                    <i class="fas fa-users fa-2x text-muted mb-3"></i>
                    <p class="text-muted">No users registered yet</p>
                  </div>
                  <% } %>
                </div>
              </div>
            </div>

            <div class="col-md-6">
              <div class="content-card">
                <div class="content-card-header">
                  <h5><i class="fas fa-history me-2"></i>Recent Sessions</h5>
                </div>
                <div class="content-card-body">
                  <% if (activities.recentSessions &&
                  activities.recentSessions.length > 0) { %> <%
                  activities.recentSessions.forEach(session => { %>
                  <div class="activity-item">
                    <div
                      class="d-flex justify-content-between align-items-start"
                    >
                      <div>
                        <strong class="text-success"
                          ><%= session.skill %></strong
                        >
                        <div class="text-muted small">
                          <i class="fas fa-clock me-1"></i><%= session.hours %>
                          hours
                        </div>
                        <div class="text-muted small mt-1">
                          <i class="fas fa-chalkboard-teacher me-1"></i>
                          <%= session.teacher?.name || 'Unknown' %> taught <%=
                          session.student?.name || 'Unknown' %>
                        </div>
                      </div>
                      <span class="badge bg-info">
                        <%= new Date(session.createdAt).toLocaleDateString() %>
                      </span>
                    </div>
                  </div>
                  <% }); %> <% } else { %>
                  <div class="text-center py-4">
                    <i class="fas fa-calendar fa-2x text-muted mb-3"></i>
                    <p class="text-muted">No sessions conducted yet</p>
                  </div>
                  <% } %>
                </div>
              </div>
            </div>
          </div>

          <!-- Top Skills -->
          <div class="row g-4 mt-2">
            <div class="col-md-6">
              <div class="content-card">
                <div class="content-card-header">
                  <h5><i class="fas fa-fire me-2"></i>Top Teaching Skills</h5>
                </div>
                <div class="content-card-body">
                  <% if (topSkills.topTeach && topSkills.topTeach.length > 0) {
                  %> <% topSkills.topTeach.forEach(([skill, count]) => { %>
                  <div
                    class="d-flex justify-content-between align-items-center py-2 border-bottom"
                  >
                    <span class="fw-bold text-dark"><%= skill %></span>
                    <span class="skill-badge"><%= count %> users</span>
                  </div>
                  <% }); %> <% } else { %>
                  <div class="text-center py-4">
                    <i
                      class="fas fa-chalkboard-teacher fa-2x text-muted mb-3"
                    ></i>
                    <p class="text-muted">No teaching skills data available</p>
                  </div>
                  <% } %>
                </div>
              </div>
            </div>

            <div class="col-md-6">
              <div class="content-card">
                <div class="content-card-header">
                  <h5>
                    <i class="fas fa-graduation-cap me-2"></i>Top Learning
                    Skills
                  </h5>
                </div>
                <div class="content-card-body">
                  <% if (topSkills.topLearn && topSkills.topLearn.length > 0) {
                  %> <% topSkills.topLearn.forEach(([skill, count]) => { %>
                  <div
                    class="d-flex justify-content-between align-items-center py-2 border-bottom"
                  >
                    <span class="fw-bold text-dark"><%= skill %></span>
                    <span class="skill-badge"><%= count %> users</span>
                  </div>
                  <% }); %> <% } else { %>
                  <div class="text-center py-4">
                    <i class="fas fa-graduation-cap fa-2x text-muted mb-3"></i>
                    <p class="text-muted">No learning skills data available</p>
                  </div>
                  <% } %>
                </div>
              </div>
            </div>
          </div>
</div>

        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
